[project]
name = "hyperion-unfinished"
version = "0.1.0"
description = "Scraper and build tooling for the Finish Hyperion web app"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
